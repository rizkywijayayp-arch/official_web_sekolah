server {
    listen 80;
    listen [::]:80;
    server_name sman40-jkt.sch.id;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name sman40-jkt.sch.id;

    root /www/wwwroot/sman40-jkt.sch.id;
    index index.html;

    ssl_certificate /etc/letsencrypt/live/sman40-jkt.sch.id/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sman40-jkt.sch.id/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json application/xml;

    location = / {
        try_files /index.html =404;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|json)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /api/ {
        proxy_pass http://127.0.0.1:5005/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API calls from frontend (with schoolId param) -> proxy to backend
    location ~ ^/(profileSekolah|heroSlides|testing|layanan|berita|pengumuman|ekstrakurikuler|alumni|guruTendik|gallery|albums|fasilitas|kurikulum|kalender|jadwal|osis|ppdb|ppid|faq|organisasi|program|rating|feed|beranda|visi-misi|pramuka|tata-tertib)$ {
        if ($arg_schoolId) {
            proxy_pass http://127.0.0.1:5005$request_uri;
            break;
        }server {
    listen 80;
    listen [::]:80;
    server_name sman40-jkt.sch.id;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name sman40-jkt.sch.id;

    root /www/wwwroot/sman40-jkt.sch.id;
    index index.html;

    ssl_certificate /etc/letsencrypt/live/sman40-jkt.sch.id/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sman40-jkt.sch.id/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json application/xml;

    location = / {
        try_files /index.html =404;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|json)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /api/ {
        proxy_pass http://127.0.0.1:5005/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API calls from frontend (with schoolId param) -> proxy to backend
    location ~ ^/(profileSekolah|heroSlides|testing|layanan|berita|pengumuman|ekstrakurikuler|alumni|guruTendik|gallery|albums|fasilitas|kurikulum|kalender|jadwal|osis|ppdb|ppid|faq|organisasi|program|rating|feed|beranda|visi-misi|pramuka|tata-tertib)$ {
        if ($arg_schoolId) {
            proxy_pass http://127.0.0.1:5005$request_uri;
            break;
        }
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    client_max_body_size 50M;
}

        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    client_max_body_size 50M;
}
